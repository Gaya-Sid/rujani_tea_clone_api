// url/api/products
const allProducts = [
  {
    product_id: product.id,
    name: product.name,
    price: product.price,
    desc: product.desc,
    media: product.media,
  },
];

// url/api/categories
